var reviewRow = "<%= j( render partial: "review", locals: { review: @review } ) %>"

var $reviewRow = $( reviewRow );

ReviewModule.inserData($reviewRow);


$("#flash-messages").html("<%= j(render partial: 'shared/flash') %>").slideDown();
<% flash.clear %>
