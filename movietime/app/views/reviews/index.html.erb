<!-- Build out the index page for all Reviews (not nested under a particular movie). It should show reviews ordered with the latest on top, much like Posts in Danebook but uglier. Each review should be a partial, to make this easier later.
Just like Posts in Danebook, there should be a review submission form at the top of the page, with an options dropdown to pick from existing movies. -->


<% @desc_reviews.each do |review| %>
  <h4>Review by: <strong><%= review.reviewer_name %></strong></h4>
  <p>Movie: <%= review.title %></p>
  <p>Review: <%= review.review_text %></p>
  <br>
<% end %>